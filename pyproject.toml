[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes
    "I",   # isort
    "DJ",  # flake8-django
    "RUF", # ruff-specific rules
]

[tool.ruff.lint.per-file-ignores]
"*/migrations/*.py" = ["E501"]
